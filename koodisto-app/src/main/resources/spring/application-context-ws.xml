<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:jaxws="http://cxf.apache.org/jaxws" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
http://cxf.apache.org/jaxws http://cxf.apache.org/schemas/jaxws.xsd">

	<!-- AUTHENTICATION / AUTHORIZATION SUPPORT -->
    <import resource="classpath:authentication-client.xml"/>

    <jaxws:client id="koodistoAdminService"
        serviceClass="fi.vm.sade.koodisto.service.KoodistoAdminService"
        address="${koodisto.admin.webservice.url.backend}" />

    <jaxws:client id="koodiAdminService"
        serviceClass="fi.vm.sade.koodisto.service.KoodiAdminService"
        address="${koodi.admin.webservice.url.backend}" />

    <jaxws:client id="koodiService"
        serviceClass="fi.vm.sade.koodisto.service.KoodiService"
        address="${koodi.public.webservice.url.backend}" />

    <jaxws:client id="koodistoService"
        serviceClass="fi.vm.sade.koodisto.service.KoodistoService"
        address="${koodisto.public.webservice.url.backend}" />

    <jaxws:client id="uploadService"
        serviceClass="fi.vm.sade.koodisto.service.UploadService"
        address="${koodi.upload.webservice.url.backend}" />

    <jaxws:client id="downloadService"
        serviceClass="fi.vm.sade.koodisto.service.DownloadService"
        address="${koodi.download.webservice.url.backend}" />

    <jaxws:client id="organisaatioService"
    	serviceClass="fi.vm.sade.organisaatio.api.model.OrganisaatioService"
    	address="${organisaatio.webservice.url.backend}" />

</beans>





